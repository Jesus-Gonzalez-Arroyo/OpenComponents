import { Component, ReactNode } from 'react';

class Header extends Component {
  render(): ReactNode {
      return(
        <div>
          <h1>Prueba del componente</h1>
          <button>Boton de prueba</button>
        </div>
      )
  }
}

export default Header;
